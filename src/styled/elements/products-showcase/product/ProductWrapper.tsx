import styled from "styled-components";
import { LighterGreenColor } from "styled/base/Colors";

export const ProductWrapper = styled.div`
  display: flex;
  flex-direction: column;
  margin: 0 0 2em 0;
  background-color: ${LighterGreenColor};
`;
