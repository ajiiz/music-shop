import styled from "styled-components";
import { Button } from "./Button";
import { DarkGreenColor } from "styled/base/Colors";

export const AddButton = styled(Button)`
  background-color: ${DarkGreenColor};
`;
